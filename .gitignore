# ===== Java / Maven =====
target/
*.class
*.jar
*.war
*.ear
*.iml
.idea/
*.ipr
*.iws
.mvn/
mvnw
mvnw.cmd

# ===== Node / Frontend =====
node_modules/
dist/
dist-ssr/
*.local
.npm
.cache/
.parcel-cache/

# ===== IDE / Editor =====
.vscode/
*.swp
*.swo
*~
.project
.classpath
.settings/

# ===== OS =====
.DS_Store
Thumbs.db
desktop.ini

# ===== Logs =====
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ===== Environment (never commit) =====
.env
.env.local
.env.*.local
# 本地密钥文件：只提交 .env.properties.example 模板，真实 Key 绝不入库
.env.properties

# ===== AI 工作区内部文件（个人记忆/笔记，不属于项目代码）=====
.workbuddy/
# AI 助手在 WorkBuddy worktree 下写的记忆目录（与 .workbuddy/ 不同名，必须单独忽略，
# 否则里面记的本机路径等信息会被 git add -A 带进仓库）
.workbuddy-ai/

# ===== Runtime uploads =====
wayfare-backend/uploads/

# ===== Temporary =====
tmp/
temp/
*.tmp
*.bak

# ===个人===
# 记忆/笔记目录：不属于项目代码，永不入库
.workbuddy/

# docs/ 与 scripts/ 不再整目录忽略（2026-09-22 改：改为按文件判定）。
# 理由：这两个目录放的是**项目交付物** —— docs 下的开发文档 / 部署说明 / 指标报告（P8-A），
# scripts 下的降级演练脚本（P7-C）都要随仓库一起交付。
# 而「整目录忽略」最坏的地方是**失败是静默的**：文件写完了、git add 也执行了，
# 但它根本没进提交树，直到部署或答辩时才发现。
# 真实踩到过：P5-D 的验收产物 wayfare-frontend/docs/界面清单.md 就是这么被挡住的。
# → 要排除个别文件请按文件逐个写，避免再恢复成目录级规则。

# 按路径排除（不是整目录规则）：上一版「光影集/摄影平台」的参考资料，
# 不属于 Wayfare 的交付物，留在本地备查即可。
docs/refs/
