# Wayfare · P7-B 量化指标采集（简历数字的来源）

> 本文件是 **P7-B 六项指标的汇总交付物**。每项包含：测量方法（可复现步骤）／原始数据表／
> 结论与简历口径／测量日期与环境。数字一律来自实测，**不好看也如实写**。
> 原始数据与采集脚本同时留档在 `scripts/p7b/`（`%TEMP%` 会丢，所以入仓）。

## 全局测量环境（每次测量都要对照这一段）

| 项 | 值 |
|---|---|
| 测量日期 | 2026-09-23（第三项跨 12:52~19:59 两段） |
| 后端 | Spring Boot 3.3.5 / JDK 17.0.20，`http://localhost:8080/api` |
| 大模型 | **glm-5.3-flash**（`reasoning-effort=low`，`max-tokens=16384`，`temperature=1`） |
| 地图 | **`map.enabled=false`** → 走 LLM 候选路径，**零百度配额消耗**，`mapMode=ESTIMATED` |
| 超时 | `llm.timeout-ms=240000`（L2 生效） |
| 重排上限 | `trip.max-replan-rounds=2` |
| 画像 | admin 的 `user_travel_profile` 存在（菜系川菜/西餐/火锅、重口味重辣、**忌口香菜**、节奏 2、步行上限 5km、`allow_ai_use=1`），每次生成都带画像注入 |
| 输入 | 固定 6 条中文自然语言输入（见 `scripts/p7b/inputs.json`），`useProfile=true` |

---

# 第三项 · 校验收散率 ✅ 已完成（30/30）

## 3.1 测量方法（可复现）

1. **清空两张日志表**（`ai_generation_log` / `external_call_log`）—— `AiLogServiceTest` 断言绝对条数，残留会让测试变红；
2. **清熔断计数**：Redis `KEYS cb:*` → `DEL`。LLM 侧熔断阈值硬编码为 **3**（`sys_config` 里没有 `llm.breaker.*` 键，
   后台改不到），一次上游抖动会连锁废掉整批样本 —— 上一轮 30 次采集里就有 **7 次**被熔断拦截（4~27ms 内失败）；
3. 启动后端（`map.enabled=false` 保持关闭）；
4. 对**固定 6 条输入**各跑 **5 轮**，共 **30 次** `POST /api/trip/plan/sync`（`{"rawInput":"…","useProfile":true}`）；
5. 每次记录：`tripId / 重排轮次 rounds / mapMode / model / durationMs / tokens / composeError / 违规数 / HIGH 数`，
   原始响应体逐份存为 `run_*.json`；
6. **归类口径（重要）**：
   - 「收敛」的判据是 `ValidationReport.passed()`，其实现是 **`violations.isEmpty()`** —— 即**零违规**才算通过，
     `rounds` 只表示重排次数，**不能单独用来判定收敛**；
   - 第 1 轮通过 = `rounds=0` 且无残留违规；
   - 第 2 轮通过 = `rounds≥1` 且无残留违规；
   - 2 轮内未通过 = **最终报告仍有残留违规**；
   - `composeError` 非空（编排本身失败）的样本单独归为「管线失败」，**不混进收敛率分母**。

> ⚠️ 踩过的口径坑：最初按「`rounds=0/1/2`」三分类，会得出「100% 收敛」，因为
> `shouldReplan()` 对**只有 MEDIUM 的违规只给一次重排机会**（`rounds > 0` 时返回 false），
> 于是「重排一次仍不通过」的样本 `rounds` 恰好是 1，被误算成「第 2 轮通过」。
> **必须以最终报告有无残留违规为准**（本文件已按此重算）。

## 3.2 原始数据（30 次有效样本）

| # | 输入 | tripId | 重排轮次 | 残留违规 | 结果 | 耗时(s) | tokens |
|---|---|---|---|---|---|---|---|
| 1 | #1 | 290 | 0 | 0 | 第 1 轮通过 | 76.8 | 5540 |
| 2 | #2 | 291 | 1 | 0 | 第 2 轮通过 | 94.2 | 8001 |
| 3 | #3 | 292 | 1 | 0 | 第 2 轮通过 | 92.5 | 7262 |
| 4 | #4 | 293 | 1 | 1 | **2 轮内未通过** | 95.9 | 7372 |
| 5 | #5 | 294 | 1 | 0 | 第 2 轮通过 | 118.5 | 5281 |
| 6 | #6 | 295 | 1 | 1 | **2 轮内未通过** | 128.8 | 12150 |
| 7 | #1 | 296 | 0 | 0 | 第 1 轮通过 | 51.6 | 4745 |
| 8 | #2 | 297 | 1 | 0 | 第 2 轮通过 | 95.1 | 7886 |
| 9 | #3 | 298 | 1 | 0 | 第 2 轮通过 | 98.6 | 5076 |
| 10 | #4 | 299 | 0 | 0 | 第 1 轮通过 | 61.2 | 6923 |
| 11 | #5 | 300 | 0 | 0 | 第 1 轮通过 | 71.1 | 5138 |
| 12 | #6 | 301 | 1 | 0 | 第 2 轮通过 | 99.7 | 5493 |
| 13 | #1 | 302 | 0 | 0 | 第 1 轮通过 | 74.1 | 5777 |
| 14 | #2 | 303 | 1 | 0 | 第 2 轮通过 | 91.0 | 7668 |
| 15 | #3 | 304 | 1 | 0 | 第 2 轮通过 | 94.1 | 7156 |
| 16 | #4 | 305 | 1 | 0 | 第 2 轮通过 | 89.4 | 4580 |
| 17 | #5 | 306 | 0 | 0 | 第 1 轮通过 | 54.8 | 6904 |
| 18 | #6 | 307 | 1 | 0 | 第 2 轮通过 | 107.8 | 8893 |
| 19 | #1 | 308 | 0 | 0 | 第 1 轮通过 | 70.9 | 5239 |
| 20 | #2 | 309 | 1 | 0 | 第 2 轮通过 | 112.6 | 5560 |
| 21 | #3 | 310 | 0 | 0 | 第 1 轮通过 | 48.6 | 8070 |
| 22 | #4 | 311 | 0 | 0 | 第 1 轮通过 | 44.8 | 4412 |
| 23 | #1 | 321 | 0 | 0 | 第 1 轮通过 | 39.0 | 4743 |
| 24 | #2 | 322 | 1 | 0 | 第 2 轮通过 | 82.8 | 8719 |
| 25 | #3 | 323 | 0 | 0 | 第 1 轮通过 | 54.1 | 4732 |
| 26 | #4 | 324 | 0 | 0 | 第 1 轮通过 | 79.3 | 2305 |
| 27 | #5 | 325 | 0 | 0 | 第 1 轮通过 | 56.2 | 4579 |
| 28 | #6 | 326 | 1 | 1 | **2 轮内未通过** | 89.0 | 7154 |
| 29 | #5 | 327 | 0 | 0 | 第 1 轮通过 | 72.5 | 7650 |
| 30 | #6 | 328 | 1 | 0 | 第 2 轮通过 | 115.4 | 5392 |

> 第 1~22 次为修复 TABOO 口径后采集（`290~311`）；第 23~30 次为本次补跑（`321~328`，输入 #1~#6 各补至 5 次，
> 补齐顺序 #1,#2,#3,#4,#5,#6,#5,#6）。两段环境完全一致：同模型、同后端、`mapMode` 全为 `ESTIMATED`。

### 分输入汇总

| 输入 | 第 1 轮通过 | 第 2 轮通过 | 2 轮内未通过 | 平均 tokens |
|---|---|---|---|---|
| #1（寿阳两天·古建筑·预算 500） | 5 | 0 | 0 | 5209 |
| #2（大同三天·古建+博物馆·不吃辣） | 0 | 5 | 0 | 7567 |
| #3（寿阳两天·带爸妈·走不动） | 2 | 3 | 0 | 6459 |
| #4（寿阳一天·寺庙·预算 300） | 3 | 1 | 1 | 5118 |
| #5（寿阳两天·古建+博物馆·节奏紧凑） | 4 | 1 | 0 | 5910 |
| #6（大同三天·古建筑·摄影·预算 2000） | 0 | 3 | 2 | 7816 |

## 3.3 结果汇总

| 指标 | 值 |
|---|---|
| 有效样本 | **30 次**（6 输入 × 5 轮） |
| 第 1 轮通过（`rounds=0`、零残留） | **14 次 = 46.7%** |
| 第 2 轮通过（`rounds≥1`、零残留） | **13 次 = 43.3%** |
| **2 轮内未通过**（最终报告仍有残留违规） | **3 次 = 10.0%** |
| **两轮内收敛率** | **27 / 30 = 90.0%** |
| 平均重排轮次 | **0.533** |
| 残留 HIGH 违规 | **0 次**（修复 TABOO 口径后，30 次全部 0） |
| 耗时 | 平均 **82.0s** ｜ 最短 **39.0s** ｜ 最长 **128.8s** |
| tokens（行程级 `meta.tokens`） | 合计 **190,400** ｜ 平均 **6,347** ｜ 最短 2,305 ｜ 最长 12,150 |
| tokens（阶段日志口径，见 3.6 缺陷） | 合计 **195,597**（输入 119,400 / 输出 76,197） |
| 本次新增 8 次的成本（glm ¥0.8/¥2.8 每百万） | 输入 30,065 + 输出 18,045 → **≈ ¥0.075** |
| 30 次合计成本 | **≈ ¥0.31**（单次均摊 **¥0.0103**） |

## 3.4 三个「2 轮内未通过」样本的归因（**全部是同一条规则**）

| tripId | 残留违规 | 严重度 | 原文（节选） |
|---|---|---|---|
| 293 | `TIME_OVERLAP` | MEDIUM | 结束时间 18:10 超出活动窗口 18:00 |
| 295 | `TIME_OVERLAP` | MEDIUM | 结束时间 20:00 超出活动窗口 18:00 |
| 326 | `TIME_OVERLAP` | MEDIUM | 结束时间 18:30 超出活动窗口 18:00 |

**这三条都是 MEDIUM，不是 HIGH。** 按 `ItineraryReplanner.shouldReplan()` 的设计：
有 HIGH → 必重排；只有 MEDIUM → **默认只重排一次**（第二轮起不再为它们消耗大模型调用）。
所以这三个样本是「重排一次后仍没修好，系统按设计停止重排、把问题如实报给用户」——
**不是死循环、不是崩溃，而是「有限次重排 + 风险清单」这条设计路径的正常输出**。

结论：**当前系统在 30 次真实生成里从未跑满重排上限（`rounds=2` 出现 0 次）**，
因为唯一能触发第 2 次重排的 HIGH 类违规一次都没出现（`0 次残留 HIGH`）。
换句话说，「2 轮内未通过」的瓶颈已从「HIGH 修不掉」变成「MEDIUM（主要是日程窗口）修不掉」。

## 3.5 结论与简历口径

- **简历口径建议**：
  「七步管线内置 7 条约束校验与自动重排，**两轮内收敛率 90%（30 次真实生成实测，6 类不同输入 × 5 轮）**；
  第 1 轮即通过占 46.7%，平均重排 0.53 轮，**30 次中残留严重违规（HIGH）0 次**；
  未收敛的 3 次全部是同一类中等问题（当日结束时间超出 18:00 活动窗口），系统按设计返回最优版本 + 风险清单。」
- **不要**写成「收敛率 100%」—— 那是把「只有 MEDIUM 时只重排一次」误当成收敛的结果（见 3.1 的口径坑）。
- **不要**只报「平均重排 0.53 轮」而不报分布 —— 46.7% / 43.3% / 10.0% 这三段才是这项指标的信息量。

## 3.6 ⚠️ 本次发现的缺陷：`meta.tokens` 不是「本次行程的 token」

**现象**：8 次补跑里 5 次的 `meta.tokens` 与 `ai_generation_log` 里该 `trip_id` 的阶段合计**对不上**。

**根因**（读代码确认）：`TripOrchestrator.buildMeta()` 用
`aiLogService.sumByUserSince(userId, runStart, LocalDateTime.now())` 取 token，
而它底层是 `sumTokensGroupByProvider(userId, since, to)` —— **按「用户 + 时间窗」过滤，不是按 `trip_id`**。
于是窗口边界一旦错位，两个方向都会错：

| tripId | 自身阶段合计 | `meta.tokens` | 差 | 精确对账 |
|---|---|---|---|---|
| 324 | 1162+1143+1997 = 4302 | **2305** | −1997 | `2305 = 1162+1143` → **漏掉自己的 COMPOSE 行**（该行 19:53:42 写入，晚于 buildMeta 的窗口右界） |
| 327 | 1206+1642+2085 = 4933 | **7650** | +2717 | `7650 = 4933 + 2717` → **计入了上一次行程（326）在 19:56:07 晚写的 COMPOSE 行**（327 的窗口从 19:56:06 起） |

**影响**：这是**度量缺陷，不是管线缺陷** —— 行程本身没问题，但
`meta.tokens` / `meta.estCost`（P7-B 第六项「单次生成成本」的取数口径）会被相邻行程污染。
本文件的 token/成本一律改用**阶段日志按 `trip_id` 聚合**的口径（`ai_generation_log` 有 `trip_id` 列，是可靠来源）。

**未修**：修法很小（`buildMeta` 改为按 `trip_id` 聚合），但它会改变已采集数据里 `meta.tokens` 一列的含义，
且属独立改动，**留待用户拍板后单独作为一个任务块处理**。

## 3.7 与「TABOO 口径修复前」的对比（同一后端、同一模型、同一输入集）

| | 修复前（trips 274~278，5 次） | 修复后（trips 290~311 + 321~328，30 次） |
|---|---|---|
| 跑满重排上限（`rounds=2`） | **5 / 5** | **0 / 30** |
| 残留 HIGH 违规 | 3 / 4 / 3 / 1 / 3 | **全部 0** |
| 两轮内收敛率 | **0%** | **90.0%** |
| 平均耗时 | 143.3s | **82.0s** |
| 平均 tokens | 10,284 | **6,347** |

> 修复内容：`TABOO` 规则原先连 `reason` 字段一起扫，于是把模型**正在执行忌口**的说明
> （「已备注店家全程不放香菜」）判成 HIGH 违规，导致重排后模型仍写同样的话 → **永远收不敛**。
> 新口径只查候选池点位名（`poiRef`）。详见 `RULES-PIPELINE.md` 与 `.workbuddy/memory/2026-09-23.md`。

### 3.8 本次留档的原始文件（`scripts/p7b/`）

| 文件 | 说明 |
|---|---|
| `convergence_runs.tsv` / `convergence_runs_part2.tsv` | 第 1~22 次 / 第 23~30 次的逐次明细（3.2 表的数据源） |
| `convergence_runs_BEFORE_FIX.tsv` | TABOO 口径修复前的 5 次（3.7 对比用） |
| `convergence_run*.txt` | 采集脚本运行日志（含每次开始时刻，是 3.6 对账的时间依据） |
| `ai_generation_log_part2_dump.tsv` | 第 23~30 次的阶段日志全量导出（**清表前的留档**） |
| `meta_tokens_defect_evidence.tsv` | 3.6 缺陷的对账证据（trips 322/324/326/327 按写入时刻排序） |
| `inputs.json` | 6 条固定输入（第三项用） |
| `run_convergence*.ps1` | 收敛率采集脚本 |
| `preflight.ps1` | 清熔断计数（Redis `KEYS cb:*` → `DEL`） |
| `inputs_harsh.json`（**未执行**） | 为「逼出 HIGH 违规」准备的跨目的地输入。**最终未执行** —— 按正确口径重算后发现已自然产生 3 个未收敛样本，无需人工构造 |
| `run_cache.ps1` / `poi_pairs.json` | 第二项待用 |
| `run_stream_metrics.ps1` | 第四、五项待用 |

---

# 第一项 · 2-opt 优化效果 ✅ 已完成

产物在 **`docs/metrics-preorder.md`**（由单元测试自动生成，勿手改）。
口径摘要：**当候选顺序存在明显交叉时，2-opt 把总里程再降约 9%~25%；顺直点集改进为 0**（5 组实测平均 9.30%，含两组 0%）。

# 第二项 · 多级缓存对响应时间的影响 ✅ 已完成（20 冷 + 20 热）

## 2.1 测量方法（可复现）

1. **临时开地图**（`POST /admin/config/map/enabled {"enabled":true}`），跑完立刻关 —— 这一步会消耗百度配额；
2. 用**同一组 POI 检索**（手册原文即「同一组」）：`city=大同 & keyword=景点 & pageNum=1 & pageSize=10`；
3. 每一轮：**先删掉该检索的 Redis 缓存键**（`map:poi*`），再连打两次同一个接口
   `GET /connector/map/search`：
   - 第 1 次 = **冷调用**（缓存未命中 → 真实打百度 → 落 `poi_cache` + 写 Redis）；
   - 第 2 次 = **热调用**（命中 Redis，不打百度）；
4. 每轮记录两次的耗时、`mode`、返回条数，并**自证清缓存真的生效**（记录清完剩余键数 `afterClear`）；
5. 脚本：`scripts/p7b/run_cache_v4.ps1`（原始数据 `e2/cache_runs.tsv` + `e2/cache_run.txt`）。

## 2.2 原始数据（20 轮，单位 ms）

| 轮 | 冷 | 热 | 轮 | 冷 | 热 | 轮 | 冷 | 热 | 轮 | 冷 | 热 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 620 | 9 | 6 | 252 | 8 | 11 | 246 | 8 | 16 | 257 | 8 |
| 2 | **1081** | 10 | 7 | 283 | 8 | 12 | 268 | 9 | 17 | 289 | 9 |
| 3 | 256 | 8 | 8 | 292 | 8 | 13 | 282 | 8 | 18 | 264 | 9 |
| 4 | 291 | 9 | 9 | 274 | 9 | 14 | 253 | 8 | 19 | 251 | 7 |
| 5 | 283 | 9 | 10 | 313 | 8 | 15 | 279 | 9 | 20 | 249 | 7 |

20 轮全部：`coldCount=10`、`warmCount=10`、`afterClear=0`（清缓存确实生效）。

## 2.3 结果汇总

| 指标 | 冷调用（打百度） | 热调用（命中 Redis） | 对比 |
|---|---|---|---|
| 平均 | **329.1 ms** | **8.4 ms** | **快 39.2×，降 97.4%** |
| 中位数 | 276 ms | 8 ms | — |
| P95 | **620 ms** | **9 ms** | 降 97.3% |
| 最短 / 最长 | 246 / 1081 ms | 7 / 10 ms | — |

## 2.4 命中证据链（**`mode` 分不出冷热，必须靠这三条**）

`MapCapabilityResolver.resolve()` 在地图开着且百度可用时**恒返回 `VERIFIED`**，
`@Cacheable` 命中时方法体根本不执行、`mode` 仍是 `VERIFIED`、响应里 `provider` 也仍写 `baidu` ——
**光看接口返回无法判断是实测还是缓存**。本项用三条独立证据锁死：

1. **`external_call_log` 行数**：跑前 4 行 → 跑后 **24 行，正好 +20**（20 次冷调各 1 条）；
   热调 **+0** → 证明 20 次热调用一次都没打百度；
2. **清缓存自证**：每轮 `afterClear=0`（删完再查，`map:poi*` 一个不剩）；
3. **落库副作用**：`poi_cache` 行数 **150 → 160（+10，即大同那 10 个点位）** ——
   冷调会落库、热调跳过落库（`@Cacheable` 命中时连 `cachePois()` 都不执行），与设计一致。

## 2.5 结论与简历口径

- **简历口径建议**：
  「POI 检索接 Redis 缓存（24h），**缓存命中响应从平均 329 ms（P95 620 ms）降至 8.4 ms（P95 9 ms），
  快 39 倍**；命中时不再打百度、也不再落库 —— 实测 20 次热调用对外 HTTP 请求为 0，
  等效把日配额 100 次的地点检索省下 50%。」
- ⚠️ **不要把「快 39 倍」当成端到端收益**：这是**单次 POI 检索**的耗时。
  一次完整规划约 6 次地点检索，按 6 × 320 ms ≈ 1.9 s 计，缓存全命中能省下约 1.9 s，
  相对整条管线 82 s 只占 2% —— **缓存的价值主要在省配额与抗限流，不在缩短整轮耗时**。这条要主动讲，否则会被追问。
- ⚠️ 第 2 轮冷调 **1081 ms** 是异常值（上游抖动），已如实保留；去掉它平均会降到 289 ms。
  **报数时用「平均 + P95」而不是只给平均**。

## 2.6 本项顺带发现的两个问题（**未修，待用户拍板**）

**① 空结果会被缓存 24 小时 —— 与代码注释写的设计意图相反。**
`CacheConfig` 的注释写着「不缓存 null：地图『这次没查到』往往只是暂时状态，若缓存 7 天，用户会连续一周都查不到」，
但 `disableCachingNullValues()` **只挡 `null`，不挡空 `List`**；而 `searchPoi` 查不到时返回的是**空 List**。
实测证据：诊断时用 `寿阳 + 古建筑` 查（百度侧 `total:0`），Redis 里留下了
`map:poi:baidu:寿阳:古建筑:1:10` 这个**缓存了空列表的键**，TTL 24h。
→ 后果与注释担心的一模一样：**用户搜一个百度查不到的词，24 小时内再搜都直接返回空**。
修法很小（`@Cacheable` 加 `unless = "#result == null || #result.isEmpty()"`），但属产品代码改动，**待拍板**。

**② `mode` / `provider` 无法区分「实测」与「缓存命中」。**
热命中时响应仍是 `mode=VERIFIED, provider=baidu`，前端角标与后台统计都会把缓存命中算成「实测」。
若要在页面上体现「这是缓存数据」，需要额外透出一个来源标记（属独立改动）。

## 2.7 配额消耗（如实报）

| 项 | 次数 |
|---|---|
| 本项正式采集（20 次冷调） | **20** |
| 诊断/干跑（查 AK 是否有效、验证清缓存修复） | 4 |
| **合计消耗** | **24 次** |
| 日配额（百度地点检索） | 100 次/天 → 占 **24%** |

> 采集期间 `map.enabled` 已恢复 **false**（脚本自动关，实测确认为 `false`）。
> 关键词选择上踩过一次坑：手册准备的 `poi_pairs.json` 用「古建筑/博物馆」，
> 但 `寿阳 + 古建筑` 在百度侧 `total:0`（`status:0` 正常响应，只是真的没数据）——
> 这也正是 ① 那个缺陷会伤到真实用户的场景。最终改用 `大同 + 景点`（`total:124`）。

# 第四项 · 流式输出的时间对比 ✅ 已完成（5/5）

## 4.1 测量方法（可复现）

1. 后端启动、`map.enabled=false`；用 `admin` 登录拿 JWT；
2. 对固定输入 **#1**（`周末想去寿阳玩两天，喜欢古建筑，预算 500`，`useProfile=true`）跑 **5 次**
   `POST /api/trip/plan/stream`（`text/event-stream`）；
3. 客户端**不能用 `EventSource`**（带不了 JWT），用 `HttpClient` + `ResponseHeadersRead` + `StreamReader` 逐行读 SSE；
   每次记录两个时刻：
   - **`itinerary` 事件到达时刻** = 骨架可交互时刻（Step6 事实补全后立即推送，也是降级出口）；
   - **`done` 事件到达时刻** = 全文（含攻略文案）完成时刻；
   同时统计 `delta` 事件的**字符数**与**块数**；
4. 另把每个 `stage` 事件（阶段 × RUNNING/DONE/FALLBACK）的到达时刻也记下来，用于解释时间花在哪；
5. 脚本：`scripts/p7b/run_stream_part1.ps1`（原始数据 `e4/part1.tsv` + `e4/part1_stages_*.txt`）。

## 4.2 原始数据

| # | `itinerary` 到达 (ms) | `done` 到达 (ms) | 差值 = 文案流耗时 (ms) | delta 字符数 | delta 块数 |
|---|---|---|---|---|---|
| 1 | 55876 | 71318 | 15442 | 586 | 420 |
| 2 | 69639 | 87220 | 17581 | 545 | 392 |
| 3 | 65474 | 82416 | 16942 | 612 | 433 |
| 4 | 114257 | 134906 | 20649 | 669 | 481 |
| 5 | 49904 | 62400 | 12496 | 454 | 319 |

## 4.3 阶段时间线（客户端观测，解释「71 秒花在哪」）

第 1 次（单位 ms）：

```
PARSE     RUNNING 626    -> DONE   9138      (8.5s)
CANDIDATE RUNNING 9138   -> DONE  22995     (13.9s)
PREORDER  RUNNING 22995  -> DONE  22996      (1ms，纯本地计算)
COMPOSE   RUNNING 22996  -> DONE  55858     (32.9s)  <- 最大头
VALIDATE  DONE   55858                      (本地规则，瞬时)
ROUTE     FALLBACK 55859 -> DONE 55861       (2ms，地图关闭 = 能力降级，不是 error)
itinerary 事件 55876                        <- 骨架此时可交互
COPY      (delta 流) 55876 -> 71318         (15.4s)
done 事件  71318
```

第 5 次（PARSE 3.2s / CANDIDATE 27.2s / COMPOSE 19.5s）→ `itinerary` 49.9s → `done` 62.4s。
**`ROUTE` 阶段发的是 `FALLBACK` 而不是 `error`** —— 这正是铁律二（地图关闭是**能力降级**，不是功能降级）在流式通道上的落地证据。

## 4.4 结果汇总

| 指标 | 值 |
|---|---|
| 骨架（`itinerary`）到达 | 平均 **71.0s** ｜ 最短 **49.9s** ｜ 最长 **114.3s** |
| 全文（`done`）到达 | 平均 **87.7s** ｜ 最短 **62.4s** ｜ 最长 **134.9s** |
| 文案流阶段耗时（骨架 → 全文） | 平均 **16.6s** ｜ 12.5s ~ 20.6s |
| 骨架占全文耗时比例 | 平均 **81.0%** |
| 文案流产出 | 平均 **573 字符 / 409 个 chunk**（≈1.4 字符/块） |
| 成功率 | **5 / 5** |

## 4.5 结论与简历口径

- **简历口径建议**：
  「行程通过 SSE 分两段交付：**结构化骨架平均约 71s 先到（可立即交互/编辑），攻略文案随后约 17s 流式补齐
  （全文平均约 88s）**；骨架占全文耗时的 81%。地图不可用时 `ROUTE` 阶段推 `FALLBACK` 事件而非 `error`，前端照常渲染。」
- ⚠️ **必须如实说明「骨架 71s 达不到手册目标 15s」**：骨架要等 **PARSE + CANDIDATE + COMPOSE 三次非流式大模型调用**
  （本项实测 PARSE 3.2~8.5s / CANDIDATE 13.9~27.2s / COMPOSE 19.5~32.9s），
  这是**架构决定的下限**，不是调优没做。要压到 15s 得换模型或合并阶段，属架构级改动。
  （`RULES-PIPELINE.md` 里早先记的「一天行程要 203 秒」是 **qwen 时代**的实测；换 `glm-5.3-flash` 后降到 71s。）
- **不要**只报「全文 88s」而不报骨架时间 —— 分段交付本身就是设计亮点，两个数要一起出现。
- ⚠️ 样本量只有 5 次且**只有 1 条输入**（输入 #1，2 天行程）；跨度较大（骨架 49.9~114.3s），
  报数时建议带区间而不是只给均值。

# 第五项 · 中断节省 tokens ⚠️ 已采集（9/9）—— **结论与手册预期不符，见 5.4**

## 5.1 测量方法（可复现）

1. 后端启动、`map.enabled=false`；固定输入 **#1**，`POST /api/trip/plan/stream`；
2. 客户端逐行读 SSE，**累计 `delta` 事件的字符数**，到达目标时取消请求（`CancellationTokenSource.Cancel()`）
   并释放 HttpClient —— 模拟浏览器断开；
3. 目标按第四项实测的整篇文案长度 573 字符折算：**20% = 115 字符、50% = 286 字符、80% = 458 字符，各 3 次，共 9 次**；
4. 两个数据源交叉：
   - **客户端**：实收字符数、中断时刻（`e5/part2_valid.tsv`）；
   - **服务端**：`ai_generation_log` 里 `stage=COPY AND success=0` 的 `error_msg`，
     形如 `已产出 N tokens，按均值估算本次节省约 M tokens；已产出 K 字符`；
   - **生效判据**：同时满足「有 `success=0` 的 COPY 行」且「`trip.guide_text` 为空」；
5. 脚本：`scripts/p7b/run_stream_part2.ps1`（带每轮前健康探活，可断点续跑）。

## 5.2 原始数据（9 次尝试）

| # | 目标 | 客户端实收 | 客户端总耗时 | trip | 服务端已产出字符 | 已产出 tokens | 自报「节省约」 | 生效？ | 证据 |
|---|---|---|---|---|---|---|---|---|---|
| 20pct-1 | 115 | 115 | 57.2s | 347 | — | — | — | ❌ **未生效** | `guide_text` **636** 字符（整篇已落库） |
| 20pct-2 | 115 | 116 | 61.5s | 348 | 582 | **416** | 435 | ✅ | `success=0`、`guide_text=0` |
| 20pct-3 | 115 | 115 | 102.6s | 349 | — | — | — | ❌ **未生效** | `guide_text` **573** 字符 |
| 50pct-1 | 286 | 286 | 63.6s | 350 | — | — | — | ❌ **未生效** | `guide_text` **588** 字符 |
| 50pct-2 | 286 | 286 | 81.4s | 351 | 529 | **378** | 434 | ✅ | `success=0`、`guide_text=0` |
| 50pct-3 | 286 | 288 | 49.1s | 352 | — | — | — | ❌ **未生效** | `guide_text` **526** 字符 |
| 80pct-1 | 458 | 458 | 101.3s | 353 | — | — | — | ❌ **未生效** | `guide_text` **518** 字符 |
| 80pct-2 | 458 | 458 | 112.3s | 354 | — | — | — | ❌ **未生效** | `guide_text` **517** 字符 |
| 80pct-3 | 458 | 458 | 82.6s | 355 | 593 | **403** | 417 | ✅ | `success=0`、`guide_text=0` |

> 另有一次 A/B 对照（trip 346，同样在 115 字符处中断）**生效**：已产出 312 tokens、自报节省 433、
> 服务端产出 445 字符 —— 用于确认「中断本身不会打挂后端」（同一次实验里完整流与中断流都跑完后端仍 HEALTHY）。
> 另 8 行**失败尝试**留在 `e5/part2.tsv`：那是沙箱中途回收了后端进程所致，**不是功能失败**（详见 5.5）。

## 5.3 生效样本的汇总（3 次）

| 指标 | 值 |
|---|---|
| 生效比例 | **3 / 9 = 33.3%**（20% / 50% / 80% 各只有 1 次生效） |
| 已产出 tokens（真实 usage） | 416 / 378 / 403 → 平均 **399** |
| 服务端自报「节省约」 | 435 / 434 / 417 → 平均 **429** |
| 服务端实际已产出字符 | 582 / 529 / 593（整篇均值约 573） |
| COPY 阶段耗时 | 15373 / 12255 / 14472 ms |
| 断开检测方式（日志原文） | `客户端断开（发送 delta 事件失败：AsyncRequestNotUsableException）` |

## 5.4 🔴 结论：手册预设的两个前提都不成立

**前提一「客户端断开即中断 LLM 流」—— 只有 1/3 成立。**
9 次里 6 次**完全没被检测到**：模型把整篇文案生成完，服务端照常写回 `guide_text`（517~636 字符，
与完整流的 454~680 同量级），日志里连一条 `success=0` 都没有。
原因（读代码）：检测点是**「下一次 `send()` 抛异常」**。SSE 的 delta 写入很小（平均 1.4 字符/块），
被 socket 发送缓冲吸收后 `send()` 会一直成功 —— 客户端已经断开，服务端却察觉不到，直到模型自己跑完。

**前提二「中断能省下 tokens」—— 数量级被高估了约 14 倍。**
`StreamCancellation.interruptionSummary()` 写的「节省约 M tokens」，
M 取的是 **`AVG(completion_tokens)` of COPY 阶段**（实测 417.4，12 条完整样本），
即**「整篇的预期成本」，并没有减去已经产出的部分**。真实净节省应为：

| trip | 整篇预期（自报值） | 已产出（已计费） | **真实净节省** |
|---|---|---|---|
| 348 | 435 | 416 | **19** |
| 351 | 434 | 378 | **56** |
| 355 | 417 | 403 | **14** |
| **平均** | **429** | **399** | **≈ 30 tokens** |

**30 tokens ≈ 一次完整生成（约 6,300 tokens）的 0.5%。** 而且这还是「生效」的那 3 次 ——
另外 6 次一分钱没省。**所以「中断节省 tokens」这个指标，本项目目前拿不出好看的数字，如实写。**

## 5.5 诚实口径（简历里**不能**怎么写）

- ❌ **不要写**「客户端断开即中断 LLM 流，单次中断平均节省约 N tokens」——
  前半句只有 1/3 成立，后半句的数字是整篇预期值、不是节省值。
- ✅ **可以写**：「客户端断开后服务端会停止生成并**拒绝落库半截文案**（9 次实测中 3 次成功掐断，
  其余因断开检测延迟未及生效）；实测净节省 token 很小（约 30 tokens）——
  **这条链路的真实价值在于不产生半成品数据，而不是省成本**。」
- ✅ 并如实说明瓶颈与改进方向：检测依赖 `send()` 失败，而 SSE 小写入会被 socket 缓冲吸收 →
  需更可靠的存活探测（如客户端主动上报取消、或服务端轮询 emitter 状态），
  否则「断开即中断」只能在连接被 RST 时偶然成立。
- ⚠️ **样本与方法的局限（必须一起说）**：n=9、单一输入；客户端是 .NET `HttpClient` 取消，
  **真实浏览器关标签页的 RST 行为可能不同**，1/3 这个比例不应直接外推。

## 5.6 顺带记录：一次「疑似崩溃」的排除过程

采集中出现**后端进程反复消失**（3 次），一度怀疑是「客户端中断打挂了后端」。已排除：

- 无 `hs_err_pid*.log`（JVM 未崩溃）、日志里 **0 条 Exception/ERROR**、代码里**没有 `System.exit`/`Runtime.halt`**
  → 进程是被**外部杀掉**的（无优雅关闭日志 = SIGKILL 特征）；
- **A/B 对照实验**（`ab/ab.txt`）：完整流跑完后 HEALTHY → 中断流跑完后仍 HEALTHY → 8080 持续监听
  → **中断不会打挂后端**；
- 结论：是**沙箱定期回收后台进程**的环境问题，与产品无关。对策：采集脚本改成**每轮前探活 + 可续跑**，
  分批（5 次 + 4 次）跑完。

# 第六项 · 单次生成成本 ✅ 已完成（3 模型 × 各 5 个有效样本）

## 6.1 测量方法（可复现）

1. 三个模型各跑 **5 次完整生成**：`POST /api/trip/plan/sync`，固定输入 **#1**（`周末想去寿阳玩两天，喜欢古建筑，预算 500`），
   `map.enabled=false`（`mapMode=ESTIMATED`，零百度配额消耗）；
2. **隔离变量**：每轮把 `llm.active-provider` 与 `llm.fallback-order` **都只留当前厂商**（链尾只剩内置 mock），
   避免某家失败时被另一家悄悄救回来；事后核对阶段日志里的 provider 集合，
   **确认 15 个有效样本里 mock 回落次数 = 0**（确实全是目标厂商自己跑的）；
3. **取数口径**：**`ai_generation_log` 按 `trip_id` 聚合**（**不用 `meta.tokens`** ——
   它按「用户 + 时间窗」查、会被相邻行程污染，见第三项 3.6 的实测证据）；
4. **成本公式**（P4-C）：`成本 = 输入 × in价/1e6 + 输出 × out价/1e6`，单价（元/百万 tokens）：

   | 模型 | 输入 | 输出 | 来源 |
   |---|---|---|---|
   | `deepseek-flash` | **1** | **4** | 官方定价页「空闲时段」价；**高峰时段翻倍（2/8）** |
   | `glm-5.3-flash` | 0.8 | 2.8 | 官方报价 |
   | `qwen3.8-flash` | — | — | **免费档 → 成本 0 元** |

5. DeepSeek 单价已按用户确认写入 `sys_config`（`llm.price.deepseek-input=1` / `-output=4`，STRING 型；
   `description` 里注明「空闲 1/4、高峰 2/8，周一至周五 9:00-12:00 与 14:00-18:00 为高峰」）。
   ⚠️ 本次测量时间 22:45~00:10（**空闲时段**），故取空闲价；
6. 脚本：`scripts/p7b/e6/run_item6.py`（可续跑 + 每轮前探活）+ `analyze_item6.py`（按 trip_id 聚合算成本）。

## 6.2 原始数据（逐次，**失败样本一并列出**）

| 模型 | # | trip | 耗时(s) | 输入 tok | 输出 tok | 合计 tok | 成本(元) | 结果 |
|---|---|---|---|---|---|---|---|---|
| deepseek | 1 | — | 19.2 | — | — | — | — | ❌ `Connection reset` |
| deepseek | 2 | 381 | 9.9 | 2951 | 1902 | 4853 | 0.01056 | ✅ |
| deepseek | 3 | 382 | 8.8 | 2972 | 1926 | 4898 | 0.01068 | ✅ |
| deepseek | 4 | 383 | 9.6 | 2949 | 1978 | 4927 | 0.01086 | ✅ |
| deepseek | 5 | 384 | 8.6 | 2955 | 1921 | 4876 | 0.01064 | ✅ |
| deepseek | 6 | — | 19.2 | — | — | — | — | ❌ `Connection reset` |
| deepseek | 7 | 399 | 9.1 | 2978 | 1910 | 4888 | 0.01062 | ✅ |
| glm | 1 | 385 | 64.7 | 3218 | 2233 | 5451 | 0.00883 | ✅ |
| glm | 2 | 386 | 96.0 | 4686 | 2998 | 7684 | 0.01214 | ✅（重排 1 轮） |
| glm | 3 | 387 | 58.4 | 3217 | 2148 | 5365 | 0.00859 | ✅ |
| glm | 4 | 388 | 39.1 | 3147 | 1263 | 4410 | 0.00605 | ✅ |
| glm | 5 | 389 | 55.0 | 2741 | 1528 | 4269 | 0.00647 | ✅ |
| qwen | 1 | 390 | 449.6 | — | — | — | — | ❌ COMPOSE 超时 |
| qwen | 2 | 391 | 269.0 | — | — | — | — | ❌ 候选池为空 |
| qwen | 3 | 392 | 426.4 | — | — | — | — | ❌ COMPOSE 超时 |
| qwen | 4 | 393 | 463.1 | 4955 | 30839 | 35794 | 0 | ✅（重排 1 轮） |
| qwen | 5 | 394 | 478.0 | 4522 | 33017 | 37539 | 0 | ✅（重排 1 轮） |
| qwen | 6 | 396 | 426.1 | 4458 | 26280 | 30738 | 0 | ✅（重排 1 轮） |
| qwen | 7 | 397 | 278.3 | — | — | — | — | ❌ 候选池为空 |
| qwen | 8 | 398 | 646.4 | 3139 | 26355 | 29494 | 0 | ✅ |
| qwen | 9 | — | 19.3 | — | — | — | — | ❌ `Connection reset` |
| qwen | 10 | 401 | 366.7 | 3078 | 21076 | 24154 | 0 | ✅ |
| qwen | 11 | 402 | 400.8 | — | — | — | — | ❌ COMPOSE 超时 |

## 6.3 结果汇总（只统计成功样本）

| 模型 | 成功 / 尝试 | 平均输入 tok | 平均输出 tok | **平均合计 tok** | **单次均摊成本** | **平均耗时** |
|---|---|---|---|---|---|---|
| **`deepseek-flash`** | **5 / 7** | 2961 | 1927 | **4,888** | **¥0.01067** | **9.2 s** |
| **`glm-5.3-flash`** | **5 / 5** | 3402 | 2034 | **5,436** | **¥0.00842** | 62.6 s |
| `qwen3.8-flash` | 5 / 11 | 4030 | 27,513 | **31,544** | **¥0**（免费档） | 476.1 s |

## 6.4 结论与简历口径

- **简历口径建议**：
  「单次完整行程生成约 **4,900 ~ 5,400 tokens**、**均摊成本 0.008 ~ 0.011 元**（不到 1 分钱）；
  双厂商热切换下，`deepseek-flash` 与 `glm-5.3-flash` 成本同量级（0.011 / 0.008 元），
  但前者快 **6.8 倍**（9.2s vs 62.6s）。」
- 🔴 **一个反直觉的结论：贵的模型反而更快、总价只差 27%** ——
  `deepseek-flash` 单价更高（1/4 vs glm 的 0.8/2.8），但它**输出 token 更少**（1927 vs 2034）、
  且没有 glm 那种「强制思考」的开销，所以单次总成本只比 glm 高 27%，速度却快 6.8 倍。
  **性价比看的是「单次总成本」，不是单价** —— 这条要主动讲。
- 🔴 **`qwen3.8-flash` 虽然免费，但已经不能用**（如实写）：输出 token 是另外两家的 **13~16 倍**
  （27,513 vs 1,927 / 2,034），耗时 476s，**失败率 6/11 = 55%**。
  推测原因：它是推理模型，**思维链计入 `completion_tokens`**，导致又慢又长。
  → 「免费」在这里是陷阱：**一次 qwen 生成的 token 量够 deepseek 跑 6.5 次**。
- ⚠️ 成本数字的口径提醒：DeepSeek 是**峰谷计价**，本次在**空闲时段**测（22:45~00:10），
  高峰时段（工作日 9:00-12:00、14:00-18:00）价格翻倍 → 单次成本上限约 **¥0.021**。报数时要带时段。

## 6.5 失败样本归因（6 次失败，全部如实记录）

| 错误 | 次数 | 分布 | 原文 | 归因 |
|---|---|---|---|---|
| `LLM_SERVER_ERROR`（约 19.2s） | 3 | deepseek ×2、qwen ×1 | `调用外部服务失败：Connection reset` | **上游瞬时断连，跨厂商都出现** → 不是某一家的问题 |
| `LLM_TIMEOUT`（240036 ms） | 4 | qwen ×4 | `大模型调用超时：请求超时` | qwen 的长输出阶段撞 `llm.timeout-ms=240000` 上限 |
| `CANDIDATE_SHORTAGE`（240033 ms） | 2 | qwen ×2 | `候选池为空：没有检索到任何可用的点位` | 同样是 qwen 太慢导致候选阶段超时 |

> 注：`LLM_TIMEOUT` 与 `CANDIDATE_SHORTAGE` 有重叠计数（同一次失败在 COMPOSE 与 CANDIDATE 各留一条日志）。
> **`Connection reset` 那 3 次发生在「切换厂商后的首次调用」**（deepseek #1/#6、qwen #9），
> 3/3 命中这个模式，但**样本太少、机制未确认**，如实记录不强行归因。

## 6.6 顺带确认：`estCost` 现在部分可用了

写入 DeepSeek 单价后，`meta.estCost` 从「几乎全为 null」变成**偶尔有值**
（deepseek #4=0.0038、#5=0.0039、#7=0.004；glm #3=0.0052），但仍不稳定。
**本表的成本一律用 P4-C 公式手算**，与 `estCost` 有出入的原因是：
① `estCost` 只统计「有单价的厂商」，而每次行程都有一条 `provider="map"` 的 ROUTE 行（map 无单价）；
② 它读的是被时间窗污染的 `meta.tokens`（见第三项 3.6）。**两个缺陷叠加，`estCost` 暂不可作为口径。**

---

# 原始数据留档索引（`scripts/p7b/`）

> `%TEMP%` 重启即失，所有原始数据与脚本已复制进仓库。第三项的文件清单见 3.8。

| 路径 | 对应 | 说明 |
|---|---|---|
| `e4/part1.tsv` | 第四项 | 5 次完整流的 `itinerary` / `done` 到达时刻、delta 字符数与块数 |
| `e4/part1_stages_*.txt` | 第四项 | 每次的 SSE `stage` 事件时间线（阶段 × RUNNING/DONE/FALLBACK） |
| `run_stream_part1.ps1` | 第四项 | 采集脚本 |
| `e5/part2_valid.tsv` | 第五项 | **9 次中断尝试的客户端数据**（实收字符、中断时刻） |
| `e5/part2.tsv` | 第五项 | 8 行**失败尝试**（沙箱回收后端所致，留作环境证据，**不计入样本**） |
| `copy_stage_rows.tsv` | 第四、五项 | **COPY 阶段全量日志导出**（12 条完整 + 4 条 `CLIENT_DISCONNECTED`），是本项两个口径的取数来源 |
| `run_stream_part2.ps1` | 第五项 | 采集脚本（每轮前探活、可断点续跑） |
| `ab/ab.txt` + `ab_test.ps1` | 第五项 5.6 | A/B 对照实验：证明「中断不会打挂后端」 |
| `ai_generation_log_part2_dump.tsv` | 第三项 | 第 23~30 次的阶段日志全量导出 |
| `meta_tokens_defect_evidence.tsv` | 第三项 3.6 | `meta.tokens` 缺陷的对账证据 |
| `convergence_runs*.tsv` / `convergence_run*.txt` | 第三项 | 30 次收敛率采集的逐次明细与运行日志 |
| `inputs.json` / `inputs_harsh.json` / `poi_pairs.json` | 通用 | 固定输入、未执行的跨目的地输入、第二项待用关键词 |
| `preflight.ps1` | 通用 | 清熔断计数（Redis `KEYS cb:*` → `DEL`） |
| `e2/cache_runs.tsv` | 第二项 | **20 冷 + 20 热逐轮明细**（耗时、mode、返回条数） |
| `e2/cache_run.txt` | 第二项 | 运行日志（含每轮 `afterClear`，自证清缓存生效） |
| `e2/external_call_log_dump.tsv` | 第二项 | 外呼日志导出（24 行 = 4 诊断 + 20 冷调；**热调 0 行**，是命中证据） |
| `run_cache_v4.ps1` | 第二项 | **实际使用的采集脚本**（显式 ContentType 登录 + 单查询 20 轮 + 每轮清缓存自证） |
| `run_cache_v3.ps1` | 第二项 | 中间版（多关键词 20 对；因 `寿阳+古建筑` 百度侧 0 结果而弃用） |
| `run_cache.ps1` / `poi_pairs.json` | — | 最初的版本（TEMP 脚本，未跑成）；`poi_pairs.json` 的「古建筑/博物馆」关键词在百度侧多城 0 结果 |
| `run_convergence*.ps1` | 第三项 | 采集脚本 |
| `run_stream_metrics.ps1` | — | 最初的合并版脚本（Part1+Part2 一次跑完）；后续已拆成 `run_stream_part1/2.ps1` |
