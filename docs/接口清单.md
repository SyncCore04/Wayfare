# Wayfare 接口清单

> ⚠️ **本文件由 `scripts/gen-api-list.py` 自动生成，请勿手改。**
> 数据来源：`controller/*.java` 的真实映射注解 + `security/JwtInterceptor.java` 的 `PUBLIC_PATHS` 常量。
> 所有路径都带 context-path `/api`，例如 `GET /works/page` 的真实地址是 `http://localhost:8080/api/works/page`。

权限列含义：

- **公开** —— 该**路径**命中 `PUBLIC_PATHS` 白名单，未登录可访问。
  ⚠️ 白名单**按路径匹配、不区分 HTTP 方法**，所以 `PUT` / `DELETE /works/{id}` 也落在白名单里 ——
  它们**能不能真的操作**由方法内部的作者校验决定（这正是「先认身份、再判放行」的设计）。
- **管理员** —— 方法体里调用了 `checkAdmin()`。
- **登录** —— 其余，需要有效 JWT。

> 局限（如实说明）：**运行期**才能判定的权限（作者本人 / 资源归属）无法从注解静态推导，
> 这类接口只会显示它的「路径级」权限。要确认某个接口的真实权限，请读方法体。

共 **105** 个端点，分布在 **20** 个 Controller。

## 运行时配置的管理接口（P1-E 定稿）（`AdminConfigController`）

类级路径：`/admin`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/admin/configs` | 管理员 | 全部配置，按 group 分组返回（后台配置页的数据源）。 |
| GET | `/admin/configs/group/{group}` | 管理员 | 按组读取，例如 GET /admin/configs/group/llm。 |
| GET | `/admin/configs/{key}` | 管理员 | 读单个配置键（敏感值掩码） |
| PUT | `/admin/configs/{key}` | 管理员 | 写单个配置键，立即生效（内部会清 sys_config 的 Redis 缓存）。 |
| POST | `/admin/config/llm` | 管理员 | 一键修改大模型相关开关。 |
| POST | `/admin/config/map/enabled` | 管理员 | 一键开关地图能力，立即生效（清缓存，不重启）。 |

## 生成统计接口（P4-C）—— 后台看板的数据源，P6 直接对接（`AdminGenerationController`）

类级路径：`/admin/generation`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/admin/generation/stats` | 管理员 | 生成统计总览：{@code GET /api/admin/generation/stats?from=&to=} |
| GET | `/admin/generation/trips/{tripId}/breakdown` | 管理员 | 某条行程的 token 与成本拆解：{@code GET /api/admin/generation/trips/{tripId}/breakdown} |
| GET | `/admin/generation/trend` | 管理员 | 每日趋势：{@code GET /api/admin/generation/trend?days=7} |
| GET | `/admin/generation/logs` | 管理员 | 生成明细分页：{@code GET /api/admin/generation/logs?from=&to=&success=&stage=&model=&destination=&mapMode=&page=&size=} |
| GET | `/admin/generation/external-calls` | 管理员 | 外部调用日志分页：{@code GET /api/admin/generation/external-calls?connector=&page=&size=} |

## 鉴权接口：注册 / 登录 / 登出（`AuthController`）

类级路径：`/auth`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/auth/register` | 公开 |  |
| POST | `/auth/login` | 公开 |  |
| POST | `/auth/logout` | 登录 | 登出：把当前 token 加入 Redis 黑名单。 |

## 分类接口（`CategoryController`）

类级路径：`/categories`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/categories/tree` | 公开 | 二级分类树（公开） |
| GET | `/categories/page` | 管理员 | 分类分页（后台管理，含禁用） |
| GET | `/categories/{id}` | 登录 |  |
| POST | `/categories` | 管理员 |  |
| PUT | `/categories/{id}` | 管理员 |  |
| DELETE | `/categories/{id}` | 管理员 |  |

## CommentController（`CommentController`）

类级路径：`/comments`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/comments` | 登录 |  |
| DELETE | `/comments/{id}` | 登录 |  |
| GET | `/comments/{id}` | 登录 |  |
| GET | `/comments/work/{workId}` | 公开 |  |
| GET | `/comments/{id}/replies` | 登录 |  |
| GET | `/comments/my` | 登录 |  |

## 连接器诊断接口（P1-E 定稿）（`DiagnosticsController`）

类级路径：`/diagnostics`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/diagnostics/connectors` | 管理员 | 两个连接器的一次性总览：开关、当前决策、熔断状态、调用统计。 |
| GET | `/diagnostics/llm/providers` | 管理员 | 已注册的厂商逐个列出配置与状态 —— 后台连接器管理页的卡片数据源。 |
| POST | `/diagnostics/llm/ping` | 管理员 | 大模型连通性测试。 |
| POST | `/diagnostics/map/ping` | 管理员 | 地图连通性测试：真实发起一次 POI 检索（走三级降级）。 |

## 收藏控制器（`FavoriteController`）

类级路径：`/favorites`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/favorites/toggle` | 登录 | 收藏/取消收藏 |
| GET | `/favorites/check` | 登录 | 检查是否已收藏 |
| GET | `/favorites/count` | 登录 | 获取作品收藏数 |
| GET | `/favorites/my` | 登录 | 获取我的收藏列表（含作品详细信息） |

## 文件上传控制器（`FileController`）

类级路径：`/files`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/files/upload` | 登录 | 单文件上传（自动添加水印） |
| POST | `/files/batch` | 登录 | 多文件上传（自动添加水印，最多9张） |
| POST | `/files/avatar` | 登录 | 头像上传（不添加水印） |

## 关注控制器（`FollowController`）

类级路径：`/follows`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/follows/toggle` | 登录 | 关注/取消关注 |
| GET | `/follows/check` | 登录 | 检查是否已关注 |
| GET | `/follows/{userId}/followers/count` | 登录 | 获取用户粉丝数 |
| GET | `/follows/{userId}/following/count` | 登录 | 获取用户关注数 |
| GET | `/follows/{userId}/followers` | 登录 | 获取用户粉丝列表 |
| GET | `/follows/{userId}/following` | 登录 | 获取用户关注列表 |
| GET | `/follows/following` | 登录 | 获取我的关注列表（含用户详细信息） |
| GET | `/follows/followers` | 登录 | 获取我的粉丝列表（含用户详细信息及是否已关注） |
| GET | `/follows/my/stats` | 登录 | 获取我的关注统计 |

## 健康检查控制器（`HealthController`）

类级路径：``

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/health` | 公开 |  |

## 点赞控制器（`LikeController`）

类级路径：`/likes`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/likes/toggle` | 登录 | 点赞/取消点赞 |
| GET | `/likes/check` | 登录 | 检查是否已点赞 |
| GET | `/likes/count` | 登录 | 获取点赞数 |
| GET | `/likes/my` | 登录 | 获取我的点赞列表 |

## 大模型连接器诊断接口（`LlmDiagnosticController`）

类级路径：`/connector/llm`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/connector/llm/status` | 管理员 | 各 Provider 的状态快照 + 本次会选中哪一家。<b>不发起任何外部调用</b>，可以随便刷。 |
| GET | `/connector/llm/ping` | 管理员 | 连通性测试：真实调用一次大模型。 |
| GET | `/connector/llm/stream-ping` | 管理员 | 流式连通性测试：把 chatStream 的每一段 delta 收集起来回传。 |

## 地图连接器诊断接口（`MapDiagnosticController`）

类级路径：`/connector/map`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/connector/map/status` | 管理员 | 当前的能力决策与熔断状态。<b>不发起任何外部请求</b>，可以随便刷。 |
| GET | `/connector/map/search` | 管理员 | 走完整降级链地做一次 POI 检索。 |
| GET | `/connector/map/route` | 管理员 | 走完整降级链地做一次路线规划。VERIFIED 命中时会把结果写进 Redis 缓存， |
| POST | `/connector/map/breaker/reset` | 管理员 | 手动重置熔断（调好 AK 之后不必干等 5 分钟） |

## 私信控制器（`MessageController`）

类级路径：`/messages`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/messages/send` | 登录 | 发送私信 |
| GET | `/messages/conversations` | 登录 | 获取会话列表（每个会话显示最后一条消息和未读数） |
| GET | `/messages/conversation/{otherUserId}` | 登录 | 获取与某用户的会话消息列表 |
| GET | `/messages/unread/count` | 登录 | 获取未读消息总数 |
| GET | `/messages/unread/count/{otherUserId}` | 登录 | 获取与某用户的未读消息数 |
| PUT | `/messages/read/{otherUserId}` | 登录 | 标记与某用户的会话为已读 |

## 推荐控制器（`RecommendController`）

类级路径：`/recommend`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/recommend/browse/{workId}` | 登录 | 记录用户浏览历史 |
| GET | `/recommend/feed` | 登录 | 首页瀑布流推荐 |
| GET | `/recommend/personal` | 登录 | 个性化推荐 |
| GET | `/recommend/hot` | 公开 | 热门作品排行榜 |
| GET | `/recommend/similar/{workId}` | 登录 | 相似作品推荐 |
| GET | `/recommend/user-tags` | 登录 | 获取用户偏好标签（调试用） |
| DELETE | `/recommend/history` | 登录 | 清除用户浏览历史 |

## TagController（`TagController`）

类级路径：`/tags`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/tags` | 管理员 |  |
| PUT | `/tags/{id}` | 管理员 |  |
| DELETE | `/tags/{id}` | 管理员 |  |
| GET | `/tags/{id}` | 登录 |  |
| GET | `/tags/page` | 登录 |  |
| GET | `/tags/hot` | 公开 |  |
| POST | `/tags/batch` | 管理员 |  |

## 用户旅行偏好画像接口（P2-A）（`TravelProfileController`）

类级路径：`/profile`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/profile/travel` | 登录 | 获取当前用户的旅行偏好画像。 |
| PUT | `/profile/travel` | 登录 | 保存当前用户的旅行偏好画像（存在则更新，不存在则插入）。 |

## 行程域接口（P3-F）（`TripController`）

类级路径：`/trip`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/trip/plan/sync` | 登录 | 同步规划：{@code rawInput} 一句话 → 完整行程落库并返回。 |
| POST | `/trip/parse` | 登录 | 只解析意图（P5-B · 前端四步流程的「确认参数」步骤）。 |
| POST | `/trip/plan/stream` | 登录 | 流式规划（P4-A）：请求体与 {@link #planSync} 完全相同，响应是 {@code text/event-stream}。 |
| POST | `/trip/{id}/regenerate-copy` | 登录 | 重新生成攻略文案（P4-B）：{@code POST /api/trip/{id}/regenerate-copy}。 |
| GET | `/trip/{id}` | 登录 | 行程详情（本人可见）。 |
| GET | `/trip/my` | 登录 | 我的行程分页列表。 |
| POST | `/trip/{id}/replan` | 登录 | 重排：只重排「第 N 天」所在范围。从 {@code feedback} 里解析出 dayIndex， |
| PUT | `/trip/{id}/days/{dayIndex}/order` | 登录 | 重排某一天的条目顺序（P5-B 的「上移 / 下移」编辑用它）。 |
| PUT | `/trip/{id}/publish` | 登录 | 把行程与已发布的攻略关联（P5-B/C · 「发布为攻略」的最后一步）。 |
| DELETE | `/trip/{id}` | 登录 | 逻辑删除行程（置 {@code trip.deleted}）。 |

## UserController（`UserController`）

类级路径：`/users`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/users/me` | 登录 |  |
| GET | `/users/{id}` | 登录 |  |
| GET | `/users/page` | 管理员 |  |
| PUT | `/users/me` | 登录 |  |
| PUT | `/users/me/password` | 登录 |  |
| PUT | `/users/{id}/status` | 管理员 |  |
| DELETE | `/users/{id}` | 管理员 |  |

## WorkController（`WorkController`）

类级路径：`/works`

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/works` | 登录 |  |
| PUT | `/works/{id}` | 公开 |  |
| DELETE | `/works/{id}` | 公开 |  |
| GET | `/works/{id}` | 公开 | 作品详情（公开，浏览量 +1）。 |
| GET | `/works/{id}/trip` | 公开 | 攻略关联的「完整行程」（P5-C · 详情页的时间轴区块）。 |
| GET | `/works/page` | 公开 | 作品分页。 |
| GET | `/works/my` | 登录 |  |
| PUT | `/works/{id}/status` | 登录 | 更新作品状态（管理员审核用）。 |
