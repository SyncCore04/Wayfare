/**
 * 统一出站治理（P1-D 落地）。
 *
 * <p>所有对外 HTTP 调用必须经过本包，集中处理：超时与重试（含指数退避）、
 * 熔断降级、结果缓存、请求/响应脱敏、外部调用日志落库。
 *
 * <p>集中收口的理由：密钥脱敏与调用成本统计如果散落在各 Provider 里，
 * 一定会漏；放在出口一处，才能保证「任何一条出站记录都不含明文 Key」。
 */
package com.wayfare.connector.governance;
