/**
 * 可插拔连接器层（P1 落地）。
 *
 * <p>把「外部依赖」抽象成可整体开关的插槽。核心要求是
 * <b>任一时刻系统都完整可用</b>：地图整体关闭后走估算模式继续全功能运行，
 * 这是<b>能力降级</b>，不是功能降级。
 *
 * <p>子包：{@code llm}（双厂商大模型）、{@code map}（百度地图与降级）、
 * {@code governance}（重试 / 熔断 / 缓存 / 脱敏 / 统一 HTTP 出口）。
 */
package com.wayfare.connector;
