/**
 * 行程域（P2-B 表族 + P3 七步编排管线）。
 *
 * <p>本包是项目核心：意图解析 → 候选检索 → 空间预排 → 行程编排 → 约束校验 →
 * 事实补全 → 结果组装。
 *
 * <p>铁律：坐标、距离、时长只能来自地图 API 或显式标记 {@code ESTIMATED}，
 * 大模型永远不允许直接输出精确公里数与分钟数。
 */
package com.wayfare.trip;
